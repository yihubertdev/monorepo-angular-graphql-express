type User {
  id: UUID
  name: String
}

input PostUserInput {
  "Create user Id"
  id: UUID

  email: EMAIL
}

extend type Query {
  hello: String
}

extend type Mutation {
  postUser(postUserInput: PostUserInput): String
}

extend type Subscription {
  subscription(postUserInput: PostUserInput): String
}
