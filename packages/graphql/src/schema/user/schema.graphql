scalar UserId

type User {
  id: String
  name: String
}

input PostUserInput {
  "Create user Id"
  id: UserId

  "Create user name"
  name: String
}

extend type Query {
  hello: User
}

extend type Mutation {
  postUser(postUserInput: PostUserInput): String
}
