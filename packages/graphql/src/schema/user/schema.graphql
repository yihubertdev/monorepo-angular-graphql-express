type user {
  id: String @StringValidation(max: 10)

  name: String @StringValidation(max: 10)
}

extend type Query {
  hello: user 
}

extend type Mutation {
  helloMutation: String @StringValidation(max: 10)
}
